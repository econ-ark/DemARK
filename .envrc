#!/usr/bin/env bash
# .envrc for DemARK repaired-scripts-after-breaking-change branch
# This automatically activates the environment when you cd into this directory

# Source conda
eval "$(conda shell.bash hook)"

# Activate the problematic combo environment
conda activate DemARK_problematic_combo

echo "🔧 Activated DemARK_problematic_combo environment for demonstrating HARK breaking changes" 